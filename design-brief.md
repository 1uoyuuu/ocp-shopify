# Only Coffee Project — Home Page Design Brief

> 状态:草稿,待确认。素材(实拍图/文案)暂用占位,后续替换。

## 1. 品牌基础

- **品牌名**:Only Coffee Project
- **业务**:咖啡豆(主打)、订阅咖啡(必做功能)、冲煮器具、周边商品
- **定位**:线下门店 + 电商(hybrid,网站需兼顾线上购买与线下门店信息)
- **品牌色**:`#0F80C1`(饱和品牌蓝,作为唯一强调色,大面积留白 + 单色点缀)
- **Logo**:
  - 图形标:两段等宽色块构成的箭头/对勾符号(↘),同时也是 wordmark 中 "ONLY" 的字母 "Y" —— 图形与文字联动,可作为贯穿全站的品牌图形符号(分隔符、hover 状态、装饰细节、列表项符号等)
  - Wordmark:几何圆体无衬线字体,笔画等宽、转角圆润,干净现代且带亲和力(不是冷峻锐利的极简)

## 2. 设计方向(参考站点提炼)

参考:noartmusic.com / phobos.com.au / oddritualgolf.com / leandra-isler.ch / mondaymorningmarket.com

提炼出的共同语言,作为 Only Coffee Project 的设计原则:

1. **大量留白,内容克制** — 每屏聚焦一件事,不堆砌信息
2. **单一强势字体驱动全场** — 一种无衬线字体做标题,克制使用等宽字体做标签/编号等细节
3. **扁平色块而非渐变/阴影** — 大面积中性色(白/黑/米白)背景 + 品牌蓝 `#0F80C1` 作为唯一点缀色
4. **图片/摄影主导内容** — 产品与门店摄影是核心视觉,而不是靠装饰元素堆砌
5. **精致微排版细节** — 编号 `[01][02]`、全大写小标签、箭头图标(呼应 logo 的 ↘ 符号)等
6. **导航悬浮透明**,叠加在 hero 图上,不抢视觉重心

整体倾向:介于 Odd Ritual(编辑感摄影 + 暖调)与 Phobos/MMM(色块自信、干净几何)之间,但因 logo 字体偏圆润亲和,整体会比 Phobos 更柔和一些,不做冷峻科技感。

## 3. Home Page 模块结构(参考 poezabride.com 的开场交互,已与用户确认)

1. **Hero — 沉浸式开场视频 + 锁定式 intro 时间轴**(单个 section,合并了视频、logo、大字标语三部分)
   - 首屏全屏视频(充满画面,无留白),大 logo 居中叠加在视频正中央
   - **滚动交互(参考 poezabride.com 复现)**:开始滚动后,页面进入一段"锁定"区间——视口视觉上像是静止的,但滚动的动作在驱动一条 GSAP 时间轴(scrub,进度与滚动距离绑定)往前播放,而不是真的在往下翻页
     - 时间轴前段:视频从全屏收缩到一个很小的居中区块(~15vw),同时大 logo 上移淡出
     - 时间轴后段(与收缩尾段有重叠):Hero 大字标语从下方淡入/上滑显现
     - 时间轴完全播放完(缩小到位 + 标语完全显现)之后,才"解锁"允许继续正常向下滚动,同时顶部 nav bar(之前一直隐藏)淡入显示,进入 sticky 状态
   - 技术实现:不用 GSAP 的 `pin`(会与主题自带的 section DOM diff 机制冲突),改用原生 `position: sticky` 在一个更高的外层容器里做出"锁定视口"的效果,GSAP 只负责在这段固定滚动距离内 scrub 数值动画(缩放/位移/透明度),不改动 DOM 结构
2. **品牌简介**
   - 简短的 Only Coffee Project 介绍文字(一段话体量,不需要展开讲理念故事,轻量带过即可)
3. **Featured Products 精选产品**
   - 产品卡片网格,克制的信息层级:产品图、名称、风味标签/产地、价格
   - 可用 logo 箭头符号做"查看全部"的视觉引导
4. **Subscription 订阅咖啡**
   - 独立展示带,说明订阅机制并给出清晰入口
5. **Footer**
   - Site index、社交媒体、联系方式、法律条款
   - 延续等宽字体 + 小标签的微排版风格

> 待定:周边商品/冲煮器具、线下门店信息未出现在本次 home page 描述中 —— 是否放进 Footer、导航菜单,或作为单独页面链接?见文末待确认问题。

## 4. 视觉与交互细节规范

- **色彩**:白/近白(米白)为主背景,黑或深色文字,`#0F80C1` 仅用于 CTA、点缀色块、hover 状态、图形标 —— 不做渐变、不做阴影投影
- **字体**:标题用几何圆体无衬线(呼应 logo wordmark 风格);正文用干净易读的无衬线;编号/标签/价格等元数据可用等宽字体强化"精品/工作室"质感
- **间距**:大留白,section 之间保持充足呼吸感,避免拥挤
- **图形符号**:logo 的 ↘ 箭头可复用为列表符号、"查看更多"箭头、分隔线装饰、hover 指示等,建立贯穿全站的识别性
- **按钮/CTA**:简洁线框或纯色块,不做圆角过度装饰,与几何圆体字体保持调性一致
- **动效**:克制的微交互(hover 状态变化、滚动淡入),不做花哨动画

## 5. 响应式 / 移动端

- 移动端保持同样的留白克制原则,但需压缩留白比例以适配小屏
- Hero 文案在移动端需缩短或换行优化,避免大标题在小屏拥挤
- 产品网格移动端切换为单列或双列

## 6. 素材状态

- 目前**暂无实拍图/产品图/最终文案**,先用占位图(色块 + 说明文字)与占位文案搭建结构
- 后续用户会提供:门店实景图、产品图、文案 —— 到位后替换占位内容,不改变模块结构

---

**已确认**:

1. Hero:沉浸式全屏视频 + 滚动收缩动画(参考 poezabride.com)
2. Subscription 模块:形式待定,先做一个简单占位展示带,细节之后再迭代
3. Sticky nav 中的 logo:用 **wordmark**(不用图形标)
4. 周边商品/冲煮器具、线下门店信息:**不在本次 home page 范围内**,后续会做独立页面,home page 暂不涉及
5. Punchy 大字标语:**态度宣言**方向,例如 "THE ONE AND ONLY COFFEE PROJECT" 这种围绕品牌名双关的宣言式文案(占位文案,后续可换)

**动效技术方案**:统一使用 **GSAP + ScrollTrigger**(已安装 gsap-skills plugin)驱动 Hero 收缩动画及其他滚动交互。
